{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stub-row",
  "type": "object",
  "title": "Generic stub-handler row payload",
  "description": "Permissive shape applied to all stub-handler routes. Validates types of common fields without forcing every route to declare a bespoke schema. Reject only obviously-wrong primitives.",
  "additionalProperties": true,
  "properties": {
    "client_id": { "type": ["string", "null"], "maxLength": 100 },
    "task_id": { "type": ["string", "null"], "maxLength": 200 },
    "request_id": { "type": ["string", "null"], "maxLength": 200 },
    "campaign_id": { "type": ["string", "null"], "maxLength": 200 }
  }
}
