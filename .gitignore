# dependencies
/node_modules
/.pnp
.pnp.js

# nested package node_modules (packages/*/node_modules from standalone installs)
packages/*/node_modules/
# nested package lockfiles — root pnpm workspace not enabled yet, so per-package
# lockfiles drift from the source of truth. Re-introduce when workspace lands.
packages/*/pnpm-lock.yaml

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# smoke-test runtime outputs
scripts/smoke-test/out/

# audit dumps (Wave 10 deep triage · ~960KB of n8n workflow JSONs)
audit-output/

# wave 14 temp diagnostic dumps (n8n executions etc · raw evidence)
.tmp-w14/

# wave 15 audit raw data (n8n workflow JSON + classifier output)
.tmp-w15/

# B-001 diagnostic dumps / debug session leftover
.tmp-b001/

# wave 17 scratch dir
.tmp-w17/

# wave 18 orphan worktree leftover
zr-w18-batch/

# claude code local settings
.claude/
